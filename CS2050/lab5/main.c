#include "lab5.h"

int main(void)
{
    /*Node * head;
    //Node ** list;

    makeList((*head));
    (*head)->data = (int*)4;

    for (int i = 0; i < 5; i++)
    {
        (*head) = insertAtHead(list, (int*)i);
    }

    Node * ptr;
    for (ptr = (*head); ptr != NULL; ptr = ptr->next)
    {
        printf("Data: %d\n", (int *)(*head)->next->data);
    }
    printf("Length: %d\n", getListLength(head));
    printf("object at 4: %d\n", getAtIndex(4, head));

    freeList(head);*/

    printf("hi!\n");

    //freeList(head);
    
}